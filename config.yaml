dataset:
  name: NIH
  root: ./data/images
  num_classes: 14

training:
  batch_size: 32
  lr: 0.0001
  epochs: 50

coreset:
  eps: 0.5
  min_samples: 5
  budget_per_class: 100

model:
  encoder: resnet18
  embedding_dim: 512
